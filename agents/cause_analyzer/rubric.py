"""반도체 FAB 병목 원인 판정 루브릭.

수치 임계값이 아닌 평가 관점을 제시한다.
LLM이 raw 수치를 이 관점으로 해석하고 판단한다.
"""

RUBRIC = """
반도체 FAB 병목 원인 판정 루브릭
==========================================

이 루브릭은 수치 기준이 아니라 평가 관점을 제시한다.
각 차원에서 제시된 raw 수치를 보고 맥락을 읽어 HIGH / MEDIUM / LOW를 판단한다.
수치 임계값을 기계적으로 적용하지 말 것.

------------------------------------------
[차원 1] SHAP 지배력
어떤 원인 카테고리가 이번 병목 예측을 주도하고 있는가?

카테고리 분류 기준:
  설비_포화  → utilization_avg, max_util 및 _delta_120 변형
  대기_누적  → q_time_min, wait_ratio 및 _delta_120 변형
  WIP_누적   → wip, wip_delta_120
  공급_부족  → available_tool_ratio

판단 관점:
  단일 카테고리가 나머지를 압도하고 있는가?
  아니면 여러 원인이 비슷한 비중으로 경합하고 있는가?
  "이 케이스의 병목은 하나의 원인으로 수렴한다"고 말할 수 있는가?
  수치를 인용해 왜 그렇게 판단했는지 설명한다.

------------------------------------------
[차원 2] 트렌드 수렴
SHAP이 지목한 카테고리의 피처가 시계열에서도 같은 방향으로 움직이고 있는가?

방향 기준:
  available_tool_ratio → 감소가 악화 방향
  그 외 모든 피처      → 증가가 악화 방향

판단 관점:
  기울기 방향, 통계 유의성, 설명력(R²)을 함께 보되,
  수치를 기계적으로 적용하지 말고 "SHAP 결론을 트렌드가 뒷받침하는가"를 판단한다.
  SHAP과 트렌드가 서로 다른 방향을 가리킨다면,
  어느 쪽이 더 신뢰할 만한지, 그 이유가 무엇인지를 판단한다.
  트렌드 데이터가 없으면 이를 불확실성 요인으로 명시한다.

------------------------------------------
[차원 3] 통계적 확인
G* 검정이 SHAP·트렌드와 독립적으로 같은 결론을 지지하는가?

배경:
  G* 검정은 포워드 시뮬레이션 결과와 베이스라인을 비교한 통계 검정이다.
  p값이 낮을수록 해당 KPI 변화가 통계적으로 유의함을 의미한다.
  delta 방향(양수=악화, available_tool_ratio는 음수=악화)도 함께 본다.

판단 관점:
  SHAP·트렌드와 무관하게, G* 결과 자체가 어떤 이야기를 하는가?
  G* 결과가 SHAP이 지목한 카테고리와 같은 방향을 독립적으로 지지하는가?
  G* 데이터가 없거나 유의하지 않다면, 이를 불확실성 요인으로 반영한다.
  p값과 delta를 인용해 판단 근거를 설명한다.

------------------------------------------
[차원 4] 미래 전망
조치 없이 방치하면 2시간 후 상황이 악화되는가?

판단 관점:
  시뮬레이션 KPI 변화 방향과 규모를 보고,
  "지금 병목이 일시적인가, 아니면 구조적으로 계속 나빠질 것인가"를 판단한다.
  단순히 수치가 얼마나 변하는지보다, 병목 맥락에서 그 변화가 의미 있는가를 본다.
  시뮬 데이터가 없다면 이를 명시하고 불확실성으로 반영한다.

==========================================
[종합 판단 지침]

네 차원의 평가 결과를 놓고,
증거들이 얼마나 일관되게 하나의 원인 카테고리를 가리키는가를
반도체 FAB 전문가 관점에서 종합 판단한다.

- 여러 차원이 같은 방향으로 수렴할수록 신뢰도가 높다.
- 차원 간 모순이 있다면, 어느 차원이 더 신뢰할 만한지 판단하고 이유를 설명한다.
- 데이터가 부족한 차원은 신뢰도를 낮추는 요인으로 반영한다.
- 신뢰도는 수식이나 규칙이 아니라, 증거 수렴 정도에 대한 전문가적 판단이다.
==========================================
"""
